<div class="d-flex align-items-center article-images mb-3">
    {% for image in article.images %}
    <div style="height: 100px; width: 100px">
        <img style="object-fit: cover; object-position: center;" src="{{asset('assets/images/' ~ image.path)}}" width="100%" height="100%" alt="">
    </div>
    {{ include('image/_delete_form.html.twig') }}
    {%endfor%}
</div>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">{{ button_label|default('Créer') }}</button>
{{ form_end(form) }}
