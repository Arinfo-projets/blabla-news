<nav class="navbar sticky-top py-3 shadow align-items-center bg-white">
    <div class="container">
        <div class="navbar-left d-flex align-items-center">
            <div class="dropdown d-sm-block d-md-none">
                <button class="dropdown-toggle btn btn-outline-secondary me-3" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                    <svg width="25" height="auto" class="MuiSvgIcon-root MuiSvgIcon-fontSizeMedium MuiBox-root css-1om0hkc" focusable="false"
                        aria-hidden="true" viewBox="0 0 24 24" data-testid="MenuIcon">
                        <path d="M3 18h18v-2H3v2zm0-5h18v-2H3v2zm0-7v2h18V6H3z"></path>
                    </svg>
                </button>
                <ul class="dropdown-menu">
                    <li><a class="dropdown-item" href="/">Articles</a></li>
                </ul>
            </div>
            <a href="#" class="fw-bold fs-3 border-start border-bottom border-3 border-primary text-danger">
                <span>BBN</span>
            </a>
            <ul class="links d-md-flex m-0 d-sm-none">
                <li class="me-3">
                    <a href="/">Articles</a>
                </li>
            </ul>
        </div>
        <div class="navbar-right d-flex align-items-center">
            {% if is_granted('ROLE_USER') and not is_granted('ROLE_ADMIN') %}
            <a href="{{path('app_contact_new')}}" class="btn btn-outline-primary me-3">
                contacter l'admin
            </a>
            {% endif %}
            <div class="dropdown avatar">
                <div style="width: 40px; height: 40px; border-radius: 90px; cursor: pointer"
                    class="shadow dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
                    {% if app.user %}
                    <img src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcQj2Q7i8sOkXInieCTCCXOLhH9-Ioo1q1nLKw&usqp=CAU"
                        alt="">
                    {% else %}
                    <img src="https://www.clipartmax.com/png/middle/269-2691367_user-login-person-man-enter-comments-logo.png"
                        alt="">
                    {% endif %}
                </div>
                <ul class="dropdown-menu" style="left: auto; right: 0" aria-labelledby="dropdownMenuButton1">
                    {% if app.user %}
                    {% if 'ROLE_ADMIN' in app.user.roles %}
                    <li><a class="dropdown-item" href="{{path('app_admin')}}">Dashboard</a></li>
                    <li><a class="dropdown-item" href="{{path('app_contact_index')}}">Message</a></li>
                    {% endif %}
                    <li><a class="dropdown-item" href="{{path('app_logout')}}">Se deconnecter</a></li>
                    {% else %}
                    <li><a class="dropdown-item" href="{{path('app_login')}}">Se connecter</a></li>
                    <li><a class="dropdown-item" href="{{path('app_register')}}">Créer un compte</a></li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </div>
</nav>

<style>
    .navbar * {
        text-decoration: none;
        list-style: none;
    }

    .navbar .links a {
        color: black;
        font-weight: 500;
    }

    .navbar .avatar img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        border-radius: 90px;
    }

    .navbar .dropdown-toggle::after {
        display: none;
    }
</style>