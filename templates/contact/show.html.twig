{% extends 'base.html.twig' %}

{% block title %}Contact{% endblock %}

{% block body %}

<div class="wrapper my-5">
    <div class="container">
        {{ include('contact/_delete_form.html.twig') }}
        <div class="mt-3">
            <ul class="list-group">
                <li class="list-group-item"><strong>De :</strong> {{contact.user.firstName}} {{contact.user.lastName}} -
                    {{contact.user.email}}
                </li>
                <li class="list-group-item"><strong>Le :</strong> {{ contact.createdAt | date('d.m.Y à H:i') }}</li>
                <li class="list-group-item">
                    <strong>Message</strong>
                    <p>{{contact.message | raw}}</p>
                </li>
                <li class="list-group-item">And a fifth one</li>
            </ul>
        </div>
    </div>
</div>

{% endblock %}